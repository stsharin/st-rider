import React from 'react';
import { Map, GoogleApiWrapper } from 'google-maps-react';

const Destination = () => {
    return (
        <div>
            <h1>Google map</h1>
        </div>
    );
};

export default Destination;